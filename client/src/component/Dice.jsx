import React from "react";

const Dice = (props) => {
  

  return (
    <div>
      here
      <i className = {`fa-solid fa-dice-${props.side}`} ></i>
    </div>

  )
}

export default Dice;
